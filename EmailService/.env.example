# Grip Email Relay - Vercel Environment Variables

# Security: Must match EMAIL_RELAY_SECRET in your main Grip Backend .env
EMAIL_RELAY_SECRET="change-this-to-a-secure-random-string"

# SMTP Configuration (Standard Gmail/SMTP settings)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-google-app-password"

# Identity
FROM_EMAIL="your-email@gmail.com"
